'use client'; import { useState } from 'react'; import { signIn } from 'next-auth/react'; import { useRouter } from 'next/navigation'; import { useTranslation } from 'react-i18next'; export default function LoginPage() { const { t } = useTranslation(); const router = useRouter(); const [error, setError] = useState(''); const [loading, setLoading] = useState(false); async function submit(form: FormData) { setLoading(true); const result = await signIn('credentials', { email: form.get('email'), password: form.get('password'), redirect: false }); setLoading(false); if (result?.error) setError(t('auth.invalidCredentials')); else router.push('/teacher'); } return <main className="page-shell"><section className="page-content flex min-h-screen max-w-md items-center"><div className="panel w-full"><p className="badge">CatchUp</p><h1 className="mt-4 text-3xl font-black text-slate-900">{t('auth.signIn')}</h1><form action={submit} className="mt-7 grid gap-4"><label className="font-medium">{t('auth.email')}<input name="email" type="email" required className="form-input" /></label><label className="font-medium">{t('auth.password')}<input name="password" type="password" required className="form-input" /></label><button disabled={loading} className="btn-primary">{loading ? t('common.loading') : t('auth.signIn')}</button>{error && <p role="alert" className="alert-error">{error}</p>}</form><a className="mt-5 block font-semibold text-sky-700 hover:text-sky-800" href="/register">{t('auth.register')}</a></div></section></main>; }
