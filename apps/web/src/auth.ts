import type { NextAuthOptions } from 'next-auth'; import CredentialsProvider from 'next-auth/providers/credentials';
export const authOptions: NextAuthOptions = { secret: process.env.NEXTAUTH_SECRET, session: { strategy: 'jwt' }, providers: [CredentialsProvider({ credentials: { email: {}, password: {} }, authorize: async (credentials) => { const response = await fetch(`${process.env.NEXT_PUBLIC_API_URL ?? 'http://localhost:3001'}/auth/login`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(credentials) }); const body = await response.json() as { data?: { user: { id: string; email: string; name?: string; role: string }; accessToken: string } }; if (!response.ok || !body.data) return null; return { ...body.data.user, accessToken: body.data.accessToken }; } })], callbacks: { jwt: ({ token, user }) => { if (user) token.accessToken = user.accessToken; return token; }, session: ({ session, token }) => { session.accessToken = token.accessToken ?? ''; return session; } } };
