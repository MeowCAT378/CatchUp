services:
  postgres-test:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: catchup_test
      POSTGRES_USER: catchup_test
      POSTGRES_PASSWORD: catchup_test
    ports:
      - "5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U catchup_test -d catchup_test"]
      interval: 2s
      timeout: 2s
      retries: 20
